def createAPI(id):
    return f"API created with {id}" 

def readAPI(id):
    return f"API read with id {id}"

def updateAPI(id):
    return f"API with id {id} updated to {id +1 }"

def deleteAPI(id):
    return f"API with id {id} deleted"